const prompt = require('prompt-sync')();

let Texte1= (prompt('Entrer un 1er caracter : '))
let Texte2= (prompt('Entrer un 2eme caracter : '))

console.log(`la chaîne résultante est :  ${Texte1}${Texte2}`) 